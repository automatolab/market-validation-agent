"""Private helpers for the Agent pipeline.

Underscore-prefixed package to signal internal use — external callers should
go through ``market_validation.services`` or the ``Agent`` facade in
``market_validation.agent``.
"""
