# Optional OpenCode defaults for this repo.
# Leave blank to use your OpenCode CLI default model/agent.
# You can override per run with:
#   batch/batch-runner.sh --model provider/model --agent general
#   python -m market_validation.batch_worker ... --model provider/model --agent general
OPENCODE_MODEL=
OPENCODE_AGENT=
